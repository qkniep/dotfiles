# hetzner — Ubuntu VPS, managed today via standalone home-manager.
#
# Will migrate to NixOS later (Phase 4 in MIGRATION.md). Until then this is a
# stub; host-specific HM bits (extra packages, host-only aliases) land here.
{ ... }:
{
}
