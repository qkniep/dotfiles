# Server profile — headless hosts (Hetzner, Ionos, …).
#
# Placeholder for now; will trim/skip GUI-oriented bits and keep the shared
# shell/CLI configuration from ../default.nix. Used by the standalone
# home-manager configurations once the Linux hosts are added.
{ ... }:
{
}
