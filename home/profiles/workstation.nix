# Workstation profile — desktops/laptops with a GUI (Mac, Linux desktop).
#
# Placeholder for now; GUI-only home configuration (terminal, desktop apps,
# WM-adjacent settings) will land here as the Linux desktop host is built out.
# The shared, headless-safe configuration lives in ../default.nix.
{ ... }:
{
}
