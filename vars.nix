# Single point of personalization.
#
# Fork-friendly: a forker changes these values (and the per-host bits under
# hosts/) and almost nothing else references identity directly. Host-specific
# settings (hostName, computerName, dock) live in hosts/<name>/.
{
  username = "qkniep";
  fullName = "Quentin Kniep";
  email = "hello@quentinkniep.com";
}
